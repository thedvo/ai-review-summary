import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    cacheComponents: true, // This enables the "use cache" directive across your application.
};

export default nextConfig;
